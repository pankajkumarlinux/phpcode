configuration:
  dependencies:
    - mvn://com.walmartlabs.concord.plugins:terraform-task:0.2.13

flows:
  default:
    - task: terraform
      in:
        action: plan
        

    - task: terraform
      in:
        action: apply
        plan: "${result.planPath}"
        # when using a previously created plan file, no need to pass `extraVars` again      